import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "echopath.interpause.github.io",
  appName: "EchoPath",
  webDir: "./dist/",
};

export default config;
